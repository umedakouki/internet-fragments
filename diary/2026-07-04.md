# 2026-07-04 コレクション日記

## 今日の主作業

直近は `feed-subscription-endpoints`、`search-avoidance-meta-tags` と、RSS、検索避け、クローラー向けの機械可読なウェブ作法が続いた。今回は同じメタデータ系でも、検索や購読ではなく、読者が本文を書かずに残す小さな反応を観察対象にした。

新規棚 `silent-reaction-buttons` は、日本語圏の個人サイトやブログで生活語になった「拍手」「スター」から始める。FC2拍手、はてなスター、Misskeyのリアクションを入口に、YouTubeの高評価、InstagramのLike、FacebookのLikeボタン、Mastodonのfavourite、ActivityPubのLike activity、GitHubとSlackのreaction APIへ広げた。

観察対象は投稿本文、作品、コメント本文ではなく、本文の外側にある反応口の名前である。拍手、スター、いいね、Favourite、emoji reaction が、ヘルプページ、仕様書、APIメソッド、外部サイト用プラグインとして残る状態を見る。

## 権利形式

10件すべて `mediaType: text`、`captureMode: linked` とした。サービス画面、ロゴ、投稿本文、利用者データは保存しない。公開URL、公開元、ページ種別、取得日、短い観察文だけを記録し、代表画像は原典スクリーンショットではなく自作SVGにした。

`rightsStatus` は、企業ヘルプやサービスページを `restricted`、公開仕様・開発者文書の再利用条件を棚内で断定しないものを `unknown` とした。いずれも `linked` なので、本文や画面を複製しない。

## 検証前メモ

作業開始時点から `AUTOMATION_INSTRUCTIONS.md` に未コミット変更がある。運用上の正本として読んだが、今回の編集・commit対象には含めない。変更対象は `data/genres/silent-reaction-buttons.json`、`data/genres/index.json`、`assets/collections/silent-reaction-buttons/reaction-buttons.svg`、この日記、自動化メモだけに限定する。

PowerShell `Invoke-WebRequest` は候補確認で「受信時に予期しないエラー」やタイムアウトになった。TLS検証は無効化せず、bundled Node.js の `--use-system-ca` 経由で候補URLのHTTP 200とページタイトルを確認した。`www.webclap.com` はタイムアウト、はてなブログヘルプのスター候補は404、Xヘルプは403、Tumblr日本語ヘルプ候補は404だったため除外した。同じ失敗では候補を水増しせず、Node system CA 経路または既存検証スクリプトのフォールバックで再試行する。

## 検証と公開

公開前の個別検証では、全15ジャンル・176標本・89保存資産の構造検証、対象10原典のリンク検証、PC・390px実ブラウザー検証がPASSした。対象棚は10件中6件が初期表示され、直接URL、3件の分岐、12ステップの重複なし探索、モバイル横溢れなし、JavaScriptエラー0件を確認した。

最初の `scripts/run-automation.ps1 -Genre silent-reaction-buttons -CommitMessage "Add silent reaction buttons collection" -Publish` は、サンドボックス内で `.git/index.lock` を作成できず、`git update-index --skip-worktree AUTOMATION_INSTRUCTIONS.md` と staging に失敗した。内容検証はすでにPASSしていたため、同じコマンドをGit書き込み可能な外側実行で再試行した。作業開始時点からある `AUTOMATION_INSTRUCTIONS.md` 差分を巻き込まないよう、外側実行中だけ `git update-index --skip-worktree AUTOMATION_INSTRUCTIONS.md` で保護し、finallyで解除した。

再試行では全検証、commit、push、Pages確認がPASSし、公開commit `7b89bdb` を `main` へpushした。GitHub Pages run `28732869380` は成功した。公開HTML、15ジャンル索引、対象ジャンルJSON10件、代表SVG、ジャンルURL、標本URLはいずれもHTTP 200だった。最終状態は `main` が `origin/main` と同期し、作業開始時点からの `AUTOMATION_INSTRUCTIONS.md` 差分だけを保持している。
