# 2026-07-02 コレクション日記

## 今日の主作業

作業開始時点で、前回選んだ新規棚 `feed-subscription-endpoints` が未コミットのまま残っていた。新しい収集へ進まず、今回の主作業はこの棚を検証し、公開可能な状態まで完了させることにした。

観察対象は前回と同じく、記事本文ではなく RSS/RDF/Atom の購読口である。日本語圏のはてなブログ、ニコニコインフォ、GIGAZINE、窓の杜、NHKニュースから始め、WordPress、Mozilla、RSS 2.0仕様、Atom RFCへ広げる構成は維持した。権利形式も `mediaType: text`、`captureMode: linked` のままとし、フィード本文や画面は保存しない。

## 検証で直した点

初回の `scripts/verify-site.ps1 -Genre feed-subscription-endpoints` は、390px表示で地図ノードが右端に寄りすぎ、`nodesFit` が false になって失敗した。内容や原典リンクの問題ではないため、`mapPosition.x` を 89 から 84 へ移し、既存ノードとの距離を保ったままモバイル表示に収めた。

修正後、次を確認した。

- `scripts/validate-genre.ps1 -All`: PASS、13ジャンル、156標本、89保存資産。
- `scripts/validate-genre.ps1 -Genre feed-subscription-endpoints -CheckLinks`: PASS、対象リンク確認済み。
- `scripts/verify-site.ps1 -Genre feed-subscription-endpoints`: PASS、PC・390px実ブラウザー、直接URL、探索12ステップ、モバイル横溢れなし。

## 公開メモ

`AUTOMATION_INSTRUCTIONS.md` には作業開始時点から未コミット差分がある。運用上の正本として読んだが、今回の公開コミットには含めない。`scripts/run-automation.ps1 -Genre feed-subscription-endpoints -CommitMessage "Add feed subscription endpoints collection" -Publish` は、指示書差分を一時的に `skip-worktree` で保護して実行した。

全検証、commit、push、Pages確認がPASSし、公開commit `ce59911` を `main` へpushした。GitHub Pages run `28543656319` は成功した。公開HTML、13ジャンル索引、対象ジャンルJSON10件、代表SVG、ジャンルURL、標本URLはいずれもHTTP 200だった。

この記録を反映するため、続けて `scripts/run-automation.ps1 -Genre feed-subscription-endpoints -CommitMessage "Record feed subscription endpoints deployment" -Publish` を実行する。
