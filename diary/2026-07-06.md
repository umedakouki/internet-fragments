# 2026-07-06 コレクション日記

## 今日の主作業

直近は `search-avoidance-meta-tags`、`silent-reaction-buttons` と、検索、反応、API、サービスヘルプに寄った非画像棚が続いた。今回は同じウェブ作法でも、設定値やボタンではなく、SNSプロフィール欄に置かれる1本のURLが複数の活動先を束ねる状態を観察対象にした。

新規棚 `profile-link-hubs` は、日本語圏の「プロフカード」「HTML名刺」から始める。TwitterやInstagramの短いプロフィールでは書ききれない自己紹介、リンク、作品置き場、支援先、連絡先を、外部の公開プロフィールへ畳み込む形式である。国内側はプロフカード、HTML名刺、Pontを置き、そこから Linktree、about.me、Carrd、Lnk.Bio、Campsite.bio、Bio Link、Bio Sites へ広げた。

観察対象は個別利用者の自己紹介文や作品ではなく、プロフィール欄から飛ぶリンク集ページという器である。古い個人サイトのリンク集や同盟バナーよりも、SNSが主な居場所になった後で、外部リンクをもう一度1枚のプロフィールへまとめ直す作法として見る。

## 権利形式

10件すべて `mediaType: text`、`captureMode: linked` とした。サービス画面、ロゴ、個別ユーザーのプロフィール本文、顔写真、作品画像は保存しない。公開URL、公開元、ページ種別、取得日、短い観察文だけを記録し、代表画像は原典スクリーンショットではなく自作SVGにした。

`rightsStatus` は全件 `restricted` とした。いずれも商用サービスの公開ページであり、本文や画面の再配布根拠を棚内で持たないため、リンク記録に留める。

## 検証前メモ

作業開始時点から `AUTOMATION_INSTRUCTIONS.md` に未コミット変更がある。運用上の正本として読んだが、今回の編集・commit対象には含めない。変更対象は `data/genres/profile-link-hubs.json`、`data/genres/index.json`、`assets/collections/profile-link-hubs/profile-hub.svg`、この日記、自動化メモだけに限定する。

lit.link は日本語圏の代表候補だが、候補確認時に公開取得が 403 になったため、今回の10件には入れなかった。POTOFU は内容確認では有効だったが、検証スクリプトのNode/system CA経路で 429 になったため除外した。Beacons と solo.to も同じ検証経路で 403 になったため、Lnk.Bio、Campsite.bio、Bio Sites に差し替えた。同じ主題で再調査する場合は、公式にHTTP 200で確認できる説明ページまたは検証スクリプトが通るURLを探し、通らなければ水増ししない。

## 検証と公開

公開前の個別検証では、全16ジャンル・186標本・89保存資産の構造検証、対象10原典のリンク検証、PC・390px実ブラウザー検証がPASSした。対象棚は10件中6件が初期表示され、直接URL、3件の分岐、12ステップの重複なし探索、モバイル横溢れなし、JavaScriptエラー0件を確認した。

最初の `scripts/run-automation.ps1 -Genre profile-link-hubs -CommitMessage "Add profile link hubs collection" -Publish` は、サンドボックス内で `.git/index.lock` を作成できず、`git update-index --skip-worktree AUTOMATION_INSTRUCTIONS.md` と staging に失敗した。内容検証はすでにPASSしていたため、同じコマンドをGit書き込み可能な外側実行で再試行した。作業開始時点からある `AUTOMATION_INSTRUCTIONS.md` 差分を巻き込まないよう、外側実行中だけ `git update-index --skip-worktree AUTOMATION_INSTRUCTIONS.md` で保護し、finallyで解除した。

再試行では全検証、commit、push、Pages確認がPASSし、公開commit `73ca76b` を `main` へpushした。GitHub Pages run `28790539204` は成功した。公開HTML、16ジャンル索引、対象ジャンルJSON10件、代表SVG、ジャンルURL、標本URLはいずれもHTTP 200だった。最終状態は `main` が `origin/main` と同期し、作業開始時点からの `AUTOMATION_INSTRUCTIONS.md` 差分だけを保持している。

この公開結果を日記へ反映するため、続けて `scripts/run-automation.ps1 -Genre profile-link-hubs -CommitMessage "Record profile link hubs deployment" -Publish` を実行する。
