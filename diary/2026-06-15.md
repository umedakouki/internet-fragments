# 2026-06-15 コレクション日記

## 今日の主作業

前日の新ジャンル追加やUI変更を続けず、分割後に7件となっていた「街角に残る仮名と旧字」を深掘りした。既存構成は変体仮名5件、旧字体1件、踊り字1件だったため、同じ蕎麦看板だけを増やさず、表示の場所と作り方が異なる3件を各familyへ1件ずつ追加した。

## 追加した標本

- 妻籠宿・吉村屋の蕎麦看板。宿場町の建物、暖簾、案内板の中で変体仮名が店の目印として残る。
- 恵比寿の戸隠そば店「なゝ樹」の置き看板。屋号の中央に平仮名の踊り字「ゝ」が使われている。
- 台湾の手書き「營業中」札。既存の印刷看板とは異なり、木札へ書かれた旧字「營」の線を比較できる。

3件ともWikimedia Commonsの原典で作者、撮影日、ライセンス、原寸画像を確認した。追加後は変体仮名6件、旧字体2件、踊り字2件の計10件となった。標本IDと原典URLを全棚で照合し、重複はない。

## 失敗と修復

Commons APIと画像配信へ短時間に照会を重ねたため、候補確認と最初の画像取得がHTTP 429で停止した。待機時間を増やし、API照会と画像取得を直列化して再試行した。恵比寿の画像はCommonsに記録された原配布元Flickrの同一写真へ切り替え、TLS検証を維持したまま取得した。妻籠宿と台湾の画像は待機後にCommonsから取得できた。

再試行時は、API照会の間を3秒以上、画像取得の間を8秒以上空け、429では指数的に待機する。Commons画像が継続して制限される場合は、原典ページに原配布元が明記された資料だけ同一写真へ切り替える。高茶屋駅の旧字「驛」は300×225pxで字形を判読できなかったため、件数へ含めず除外した。取得不能の候補を残していないため `frozen` にはしない。

## 作業ツリー

開始時に前回の公開結果を追記した `diary/2026-06-14.md` が未コミットで残っていた。内容を戻さず、今回の変更とともに検証・公開対象へ含める。

## 検証と公開

- 全6ジャンル、79標本、79保存資産の整合性が通り、追加3原典はすべてHTTP 2xxで応答した。
- 実ChromeでPC・390px、対象棚10件、直接URL、タグ絞り込み、前後移動、13件の重複なし探索、履歴、横溢れなし、JavaScriptエラー0件を確認した。
- 最初の完了コマンドは実行ラッパーの5秒上限で表示上タイムアウトしたが、子処理は継続してcommit `a510eca` を作成していた。Gitロックが残っていないことを確認し、十分な実行時間で同じコマンドを再実行した。
- 再実行は差分なしとローカルcommitを確認して `main` へpushした。GitHub Pages run `27517147086` が成功し、公開HTML、6ジャンル索引、10件の対象JSON、代表画像、ジャンルURL、標本URLはHTTP 200だった。

同じ症状では、短い実行枠で完了コマンドを重ねず、まず `.git/index.lock` と進行中プロセス、`git status --short --branch` を確認する。ローカルcommitが作成済みなら、十分な実行時間で完了コマンドを再実行し、差分なし確認からpush・Pages検証へ進める。

日記追記後のPages確認では、最初の3回だけDOM生成前に探索パネルの `dataset` を読む既知の一時的なTypeErrorが出た。完了スクリプトの再試行で回復し、run `27517184840` と公開10件を確認できた。同じ症状が再発した場合は、公開内容を変更せず `scripts/verify-pages.ps1 -Genre street-letterforms` を再実行する。

## 追補：公開タグUIを削除

ジャンル選択と3方向の探索だけで現在の規模を十分に回遊でき、タグ絞り込みは操作を増やす割に対象が少なかったため、公開画面のタグメニュー、一覧カードのタグ列、ジャンル詳細のタグボタンを削除した。標本のfamily・媒体による絞り込みは棚内の比較に必要なため維持している。

JSON内の `tags` は削除していない。共有タグは地図配置、関係線の強さ、「似たもの」探索、今後の棚再編に引き続き使う。変更は公開操作だけで、79標本のデータ、原典、権利情報、ジャンル関係に変更はない。

同時に探索画面のスクロールを調整した。PCでは右側の詳細を最下部まで送った後にページ本体まで動かないよう、詳細パネル内でスクロール連鎖を止めた。スマホではピンチ拡大を追加せず、560×640pxだった地図面を表示枠へ収め、全ジャンルを一度に見渡せる寸法へ変更した。1本指の縦操作はページスクロールへ渡し、地図操作と下方向の移動が競合しないことを優先した。

実Chromeで、公開タグUIが0件、内部タグによる13件の重複なし探索が維持されること、PC詳細のスクロール連鎖が `contain` で止まることを確認した。390pxでは地図面と全ジャンルノードが表示枠内に収まり、`scrollWidth = clientWidth = 390`、1本指の縦操作は `pan-y`、JavaScriptエラー0件だった。Codex内ブラウザーは既知のWindows権限エラーで起動できなかったため、インストール済みChromeのCDP検証とPC・390pxスクリーンショットの目視へ切り替えた。

commit `0796b02` を `main` へpushし、GitHub Pages run `27518603605` が成功した。公開HTML、タグUIを持たない対象棚、10件JSON、代表画像、ジャンルURL、標本URLはHTTP 200だった。Pages確認の最初の試行はジャンルノードの待機タイムアウト、次はDOM生成前の `dataset` TypeErrorで停止したが、完了スクリプトの再試行で回復した。同じ症状では公開内容を変更せず、`scripts/verify-pages.ps1 -Genre street-letterforms` を再実行する。

## 追補：影で時を読む日時計

直近の既存棚の深掘りとUI変更を続けず、別媒体の新ジャンルとして「影で時を読む日時計」を開始した。The Metropolitan Museum of ArtのOpen Access APIから、日時計そのものが主機能で、Public Domain表示と公開画像を確認できる10件を選んだ。

内訳は据置盤2件、携帯水平盤2件、方位磁針盤2件、折畳み盤2件、垂直盤1件、日時計校正器を備えた複合器1件。作者、制作年、素材、寸法、所蔵情報、原典URLを公式APIで確認し、600px級の公開画像を直列で取得した。時計、火薬入れ、装飾品など、日時計が副機能に留まる候補は除外した。

### frozen候補：Plan of Chicagoの都市計画図

最初はArt Institute of Chicagoの1909年『Plan of Chicago』から、都市成長、鉄道、道路、港湾を描く計画図10件を選定した。公式APIでは全件の作者、制作年、技法、Public Domain表示、IIIF画像IDを確認できたが、`www.artic.edu/iiif/2/...` の画像取得が全件CloudflareのJavaScript確認へ転送され、自動収集できなかった。0バイト資産は残しておらず、空の作業ディレクトリも削除する。

再試行時は、AIC公式APIのメタデータ取得後、公式IIIFが非対話クライアントへ画像を返す状態かを1件だけ確認する。843px画像がHTTP 200かつJPEGとして取得できた場合のみ、1秒以上の間隔で直列取得する。Cloudflare確認が続く間は件数へ含めず `frozen` のままとし、TLS検証の無効化や非公式ミラーへの切替は行わない。

対象原典の単独検証をサンドボックス内で実行した際は、WindowsのTLS資格情報を取得できず、Met API 10件がすべて「受信時に予期しないエラー」で停止した。JSON、資産、原典自体の不備ではなく、同じ `scripts/validate-genre.ps1 -Genre sundials-reading-shadow -CheckLinks` を外側の実行環境で再実行すると、7ジャンル、89標本、89資産、対象10リンクがPASSした。同じ症状ではTLS検証を無効化せず、外側の実行環境で同じコマンドを再試行する。

最初の完了コマンドでは、日時計ジャンルを地図上端の `y: 13` に置いたため、390px表示で100px幅のジャンルノード上部が地図枠を越え、`nodesFit=false` で実ブラウザー検証が停止した。公開やcommitには進んでいない。座標を左上寄りの `x: 22, y: 20` へ移し、既存ノードとの距離とモバイル表示枠の両方を確保してから同じ完了コマンドを再実行する。

修正後は全7ジャンル、89標本、89資産と対象10原典がPASSした。実ChromeでPC・390px、日時計棚10件、直接URL、前後移動、13件の重複なし探索、履歴、地図内への全ノード収まり、横溢れなし、JavaScriptエラー0件を確認した。commit `a6d63c3` を `main` へpushし、GitHub Pages run `27519342246` が成功した。公開HTML、7ジャンル索引、10件の日時計JSON、代表画像、ジャンルURL、標本URLはいずれもHTTP 200だった。

## 追補：日本で影を読む方法

直近の新ジャンル追加と画像中心の収集を繰り返さず、「影で時を読む日時計」を文章・ウェブ標本で深掘りした。観察対象を器物の形ではなく、影を土地、季節、方角、時刻へ結び直す読み方に置いた。

セイコーミュージアム銀座の公開ページから3件を追加した。江戸後期の紙製日時計は、時の鐘が届かない場所で季節札を立て、影の長さを読む。地域ごとの日の長さに合わせた盤が道中案内へ付属したという説明から、日時計が旅と土地に結び付いた道具だったことを採集した。清朝の赤道型日時計は、北極星へ向けた棒、赤道に平行な盤、表裏の十二支目盛を季節で使い分ける構成を、既存の欧州製携帯器と比較する。2026年6月9日から7月19日まで東京で開催される企画展は、収蔵日時計、江戸の紙製盤の復刻、現代デザイン、天文学を同じ展示へ置く現在の受け継ぎ方として記録した。

3件は原典サイトの著作権表示に従い、本文、展示画像、収蔵品画像を保存していない。`rightsStatus: restricted`、`captureMode: linked` とし、自分の観察文、必要なメタデータ、公開URLだけを収録した。棚は画像10件、文章1件、リンク2件の計13件となった。

原典確認時、Windows PowerShellの `Invoke-WebRequest` はサンドボックス内で `SEC_E_NO_CREDENTIALS` によりTLS接続できなかった。TLS検証を無効化せず、Node.jsの `--use-system-ca` では同じMet APIとセイコーミュージアムのURLがHTTP 200を返したため、原典検証にこの経路をフォールバックとして追加した。再試行時は `INTERNET_FRAGMENTS_NODE` にNode実行ファイルを指定できる。Windows PowerShell側でHTTP応答が得られる環境では従来経路を優先する。

全7ジャンル、92標本、89保存資産の構造検証と、対象13原典のリンク検証はPASSした。実ChromeではPC・390px、日時計棚13件、文章・リンク標本を含む13件の重複なし探索、直接URL、前後移動、履歴、地図内への全ノード収まり、横溢れなし、JavaScriptエラー0件を確認した。

完了コマンドは検証後の `git add -A` で `.git/index.lock: Permission denied` となり、commit前に停止した。ロックファイルは残っていない。直接の `git add -A` も同じ制約で停止し、GitHubへの `git ls-remote` はサンドボックスのSchannel資格情報エラー、`gh auth status` は保存トークン失効を報告した。GitHub connectorは読み取りには成功したが、tree作成とbranch作成が `Resource not accessible by integration` のHTTP 403で、代替公開経路には使えなかった。

権限と認証が利用できる環境で、次を再実行する。内容の再収集は不要で、同じ検証からcommit・push・Pages確認へ進められる。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/run-automation.ps1 -Genre sundials-reading-shadow -CommitMessage "Deepen sundials through Japanese shadow readings" -Publish
```

サンドボックス内での再試行は同じ `.git/index.lock: Permission denied` で停止したが、承認済みの外側実行環境で同じ完了コマンドを再実行して解消した。構造検証は7ジャンル・92標本・89保存資産、対象13原典がPASSし、実ChromeでもPC・390px、13件の重複なし探索、直接URL、横溢れなし、JavaScriptエラー0件を再確認した。

commit `bc606ef` を `main` へpushし、GitHub Pages run `27520676660` が成功した。公開HTML、7ジャンル索引、13件の日時計JSON、代表画像、ジャンルURL、標本URLはいずれもHTTP 200だった。今後 `.git/index.lock` の権限拒否が再発した場合は、ロックが残っていないことを確認してから、内容を変更せず同じ `scripts/run-automation.ps1 -Publish` を承認済みの外側実行環境で再試行する。

## 追補：古書体看板の選外理由を残す

直近の新規収集、既存棚の深掘り、UI変更を続けず、今回は標本移動を主作業にした。「欧州の古書体看板」の異質なfamily「歴史的字形」2件を画像で見直し、字形そのものを公開棚の採否基準にした。

Herzberg am Harzの `Junkernstraße` 表示は、通り名に明確なブラックレターを使い、その下の解説をサンセリフで組んでいる。同じ表示内で歴史性と可読性が役割分担されているため、familyを「歴史的字形」から「ブラックレター」へ直した。一方、Norderneyの `Marienhöhe` は2025年の現存施設だが、画像で確認できる表示は単純な大文字サンセリフで、古書体とは判断できない。観光地の建築や夜景が古さを想起させることと、文字の形を混同していたため、公開棚から非公開の比較外資料へ移した。

非公開棚は「古書体看板の比較外資料」へ改称した。1920年代の歴史写真と、字形判定で外れた現代表示を、原典・権利・保存資産とともに理由付きで保持する。公開棚は11件となり、familyはフラクトゥール8件、ブラックレター3件に整理された。移動した標本と再分類した標本、および触れた既存比較資料へ `rightsStatus: clear` と `captureMode: stored` を付けた。新しい資料の収集や画像取得は行っていない。

全7ジャンル・92標本・89保存資産の構造検証と、公開棚11件・非公開棚2件の原典リンク検証はPASSした。実ChromeではPC・390px、公開棚11件、直接URL、13件の重複なし探索、履歴、地図内への全ノード収まり、横溢れなし、JavaScriptエラー0件を確認した。

最初の完了コマンドは検証後の `git add -A` で `.git/index.lock: Permission denied` となり、commit前に停止した。ロックファイルは残っておらず、内容の不備ではなくサンドボックスのGit書込制約だった。承認済みの外側実行環境で同じ `scripts/run-automation.ps1 -Publish` を再実行して解消し、commit `3c52351` を `main` へpushした。GitHub Pages run `27520975156` は成功し、公開HTML、7ジャンル索引、11件の対象JSON、代表画像、ジャンルURL、標本URLはいずれもHTTP 200だった。同じ失敗ではロック残存を確認してから、内容を変更せず同じ完了コマンドを外側環境で再試行する。

## 追補：海藻標本から日本の青写真技術へ

直近の看板棚の移動・非公開化や日時計の追補を繰り返さず、画像10件だけだった「青に沈む海藻標本」を文章とアーカイブ記録で深掘りした。観察対象を海藻の形からシアノタイプ技法の用途移動へ広げ、棚を「青に写す標本と図面」へ改題した。

日本語圏の出発点として国立国会図書館サーチで「青写真」を調べ、原典へ直接辿れる4件を選んだ。1932年に東京帝国大学から内務省へ渡した図面の控え、1936年の青写真焼付機械の光源、1950年の青写真感光紙、1985年の化学教育のデモ実験である。植物を紙へ直接置いたアンナ・アトキンスの像から、図面複写、感光紙の工業生産、天候に依存しない焼付機、光化学を見せる教育実験へ、青い像の役割が移る順路を同じ棚で読めるようにした。

東京大学文書館とJ-STAGEの本文、PDF、画像は保存していない。4件とも自分の観察文、書誌情報、原典URLだけを記録し、東京大学文書館の利用条件を個別に再利用判断していない資料は `rightsStatus: unknown`、J-STAGEの3件は `rightsStatus: restricted`、すべて `captureMode: linked` とした。棚は画像10件、文章3件、リンク1件の計14件となった。

全7ジャンル・96標本・89保存資産の構造検証と、対象14件の原典リンク検証はPASSした。実ChromeではPC・390px、対象棚14件、文章・リンク標本を含む13件の重複なし探索、直接URL、前後移動、履歴、地図内への全ノード収まり、横溢れなし、JavaScriptエラー0件を確認した。

最初の完了コマンドは検証後の `git add -A` で `.git/index.lock: Permission denied` となり、commit前に停止した。ロックファイルは残っておらず、内容の不備ではなくサンドボックスのGit書込制約だった。承認済みの外側実行環境で同じコマンドを再実行し、commit `4ccf4a0` を `main` へpushした。GitHub Pages run `27521436663` は成功し、公開HTML、7ジャンル索引、14件の対象JSON、代表画像、ジャンルURL、標本URLはいずれもHTTP 200だった。

Pages確認の途中で、DOM生成前にジャンルノードの `dataset` を読む既知の一時的なTypeErrorが一度出たが、完了スクリプトの組み込み再試行で回復し、公開検証はPASSした。同じ症状では公開内容を変更せず `scripts/verify-pages.ps1 -Genre anna-atkins-cyanotypes` を再実行する。Git書込権限の失敗では `.git/index.lock` が残っていないことを確認し、同じ `scripts/run-automation.ps1 -Publish` を承認済みの外側環境で再試行する。

## 追補：保険標章を地域から見え方へ並べ直す

直近の新規棚、既存棚の追補、標本移動を繰り返さず、「壁に残る火災保険標章」の分類軸を再編した。観察対象は国別の違いではなく、契約建物の壁で会社を識別させるとき、文字、紋章、象徴像のどれが先に目へ入るかとした。

東京の安田火災海上保険標章を出発点に20点の保存画像を再確認した。明治期の東京に入った保険制度が会社名と社章を金属板へまとめた例から、上海、マニラ、シンガポール、イスタンブール、欧州、北米、オークランド、ハバナへ辿り、従来の地域別 `family` を「社名・文字」6件、「紋章・社章」8件、「人物・動物・道具」6件へ変更した。地域情報は題名とタグに残し、比較の入口だけを視覚伝達へ移した。

新しい資料や画像は取得していない。既存の20画像と原典メタデータを用い、棚の説明、方法、履歴、索引を更新した。全標本はWikimedia Commonsの原典記録でPublic Domain表示を確認済みの保存資産であるため、触れた20件を `rightsStatus: clear`、`captureMode: stored` へ移行する。

全7ジャンル・96標本・89保存資産の構造検証と、対象20原典のリンク検証はPASSした。実ChromeではPC・390px、対象棚20件、3つのfamily絞り込み、直接URL、13件の重複なし探索、履歴、横溢れなし、JavaScriptエラー0件を確認した。

最初の完了コマンドは検証後の `git add -A` で `.git/index.lock: Permission denied` となり、commit前に停止した。ロックファイルは残っておらず、内容の不備ではなくサンドボックスのGit書込制約だった。承認済みの外側実行環境で同じコマンドを再実行し、commit `efad7f1` を `main` へpushした。GitHub Pages run `27552005102` は成功し、公開HTML、7ジャンル索引、20件の対象JSON、代表画像、ジャンルURL、標本URLはいずれもHTTP 200だった。

Pages確認の組み込み再試行中に、DOM生成前の要素から `dataset` を読む既知の一時的なTypeErrorが一度記録された。公開後に内容を変更せず `scripts/verify-pages.ps1 -Genre fire-insurance-marks` を単独再実行すると即時PASSした。同じ症状ではデータやUIを編集せず、同じ単独検証を再実行する。
